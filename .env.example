# The API reads its env from apps/api/.env — see apps/api/.env.example.
# (Prisma loads DATABASE_URL from there too.)
#
# Quick start:
#   cp apps/api/.env.example apps/api/.env
#   pnpm db:up && pnpm db:migrate

# Copy to .env and fill in. Never commit .env.
# --- apps/api ---
NODE_ENV=development
PORT=3000

# Postgres (added in the Prisma-schema task; placeholder for now)
# DATABASE_URL=postgresql://user:password@localhost:5432/cece?schema=public

# Auth / JWT (added in the Auth task)
# JWT_SECRET=change-me

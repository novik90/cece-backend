{
  "name": "@cece/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Server-authoritative snooker engine (rules + scoring). Shared with the web client.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@cece/contract": "workspace:*"
  }
}

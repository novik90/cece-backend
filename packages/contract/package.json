{
  "name": "@cece/contract",
  "version": "0.0.0",
  "private": true,
  "description": "Single source of truth: request/response/event types and zod schemas shared by API and clients.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
